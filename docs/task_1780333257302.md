# task_1780333257302 — README: セットアップ手順・基本的な使い方・制限事項の確認と整備

**Type:** DOCS  
**完了日:** 2026-06-02  
**担当:** AI-Worker-Bot

---

## 要件

> 最低限の README を作成する。セットアップ手順・基本的な使い方・制限事項を記載する。

## 作業内容

### 確認結果

`README.md`（339行）は要件の3セクションをすべて網羅済み。追加実装は不要と判断。

| 要件 | 対応セクション | 行 |
|------|--------------|-----|
| セットアップ手順 | `## セットアップ手順`（STEP 1〜5） | L84–L149 |
| 基本的な使い方 | `## 使い方`（基本フロー・コマンド一覧） | L158–L248 |
| 制限事項 | `## 制限事項`（9項目） | L288–L300 |

### 各セクションの概要

#### セットアップ手順（STEP 1〜5）

1. Node.js 18以上のインストール
2. Claude Code のインストール（`npm install -g @anthropic-ai/claude-code`）
3. Discord Bot の作成（Developer Portal → TOKEN 取得 → サーバー招待）
4. 環境変数の設定（`.env.example` → `.env` にコピーして編集）
5. 起動（`npm start`）

#### 基本的な使い方

```
!project create <名前>   → プロジェクト作成
!project run <名前>      → 自律ループ開始
!approve / !deny <id>    → HUMAN_CHECK への応答
!project stop <名前>     → 停止
```

状態確認コマンド（`!project runner status` / `!quality status` / `!task list` 等）も記載済み。

#### 制限事項（9項目）

| 項目 | 内容 |
|------|------|
| 動作 OS | Windows 専用 |
| Claude Code | Anthropic APIキー必要・トークン課金あり |
| OpenAI API | `OPENAI_API_KEY` がなければ Codex レビュー不可 |
| GitHub Push | `GITHUB_TOKEN` がなければ自動 Push 無効 |
| 同時実行 | 1プロジェクトのみ（並列ループ非対応） |
| タスク実行時間 | タイムアウト 5分。大きな実装は事前分割が必要 |
| コマンド実行元 | `ALLOWED_CHANNEL_IDS` 外からのコマンドは無視 |
| Bot 再起動 | PC スリープ/シャットダウンで停止 |
| 言語 | 日本語インターフェース |

### 設計判断

- README は既に Phase F-4 対応の完全版（339行）として整備済み
- 前タスク（task_1780333257301）で「5分クイックスタート」セクションも追加済み
- 本タスクでは実装コードの変更なし（DOCS タイプガード厳守）
- 不足セクションなし → README の新規変更は行わず、本タスクドキュメントのみ作成

---

## git 証跡

```
$ git status
docs/task_1780333257302.md  ← 新規作成（本ファイル）
```

**ステータス: 完了**
